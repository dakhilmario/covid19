/* Source and licensing information for the line(s) below can be found at http://localhost/corona-website/covid19/core/modules/system/js/system.modules.js. */
(function($,Drupal,debounce){Drupal.behaviors.tableFilterByText={attach:function attach(context,settings){var $input=$('input.table-filter-text').once('table-filter-text'),$table=$($input.attr('data-table')),$rowsAndDetails,$rows,$details,searching=false
function hidePackageDetails(index,element){var $packDetails=$(element),$visibleRows=$packDetails.find('tbody tr:visible');$packDetails.toggle($visibleRows.length>0)}
function filterModuleList(e){var query=$(e.target).val(),re=new RegExp("\\b".concat(query),'i')
function showModuleRow(index,row){var $row=$(row),$sources=$row.find('.table-filter-text-source, .module-name, .module-description'),textMatch=$sources.text().search(re)!==-1;$row.closest('tr').toggle(textMatch)};$rowsAndDetails.show();if(query.length>=2){searching=true;$rows.each(showModuleRow);$details.not('[open]').attr('data-drupal-system-state','forced-open');$details.attr('open',true).each(hidePackageDetails);Drupal.announce(Drupal.t('!modules modules are available in the modified list.',{'!modules':$rowsAndDetails.find('tbody tr:visible').length}))}else if(searching){searching=false;$rowsAndDetails.show();$details.filter('[data-drupal-system-state="forced-open"]').removeAttr('data-drupal-system-state').attr('open',false)}}
function preventEnterKey(event){if(event.which===13){event.preventDefault();event.stopPropagation()}};if($table.length){$rowsAndDetails=$table.find('tr, details');$rows=$table.find('tbody tr');$details=$rowsAndDetails.filter('.package-listing');$input.on({keyup:debounce(filterModuleList,200),keydown:preventEnterKey})}}}})(jQuery,Drupal,Drupal.debounce)
/* Source and licensing information for the above line(s) can be found at http://localhost/corona-website/covid19/core/modules/system/js/system.modules.js. */